# Copy this file to docker-compose.override.yml and adjust as needed.
# This file is git-ignored — safe for developer-specific overrides.

services:
  postgres:
    ports:
      - '5432:5432'
    # Uncomment to persist data to a local path instead of named volume:
    # volumes:
    #   - ./data/postgres:/var/lib/postgresql/data

  redis:
    ports:
      - '6379:6379'
